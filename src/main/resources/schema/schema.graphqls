type Query {
    users(username: String): [User]
    testingsByUserUsername(username: String!): [Testing]
}

type User {
    id: ID
    username: String
}

type Testing {
    id: ID
    title: String
    User: User
}